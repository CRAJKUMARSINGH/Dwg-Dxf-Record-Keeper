kind = "web"
previewPath = "/"
title = "Bridge Design Suite"
version = "1.0.0"
id = "artifacts/bridge-design-suite"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21848

[services.development]
run = "pnpm --filter @workspace/bridge-design-suite run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/bridge-design-suite", "run", "build" ]
publicDir = "artifacts/bridge-design-suite/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21848"
BASE_PATH = "/"
